# Only for Ubuntu/Debian.
# apt list --installed > pckgs.txt
class SystemPackages():

    installed:qpackage_names = []

    def isInstalled(self, package_name):
            print(package_name)

    for p in package_names:
        self.isInstalled(p)


if __name__ == '__main__':
    main()

